import AdminDashboard from '@/components/admin/AdminDashboard';

export const metadata = {
  title: 'Admin Dashboard | DeepFake Detection Research Lab',
  description: 'Administrative dashboard for managing users, researcher verification, and platform access.',
};

export default function AdminPage() {
  return <AdminDashboard />;
} 